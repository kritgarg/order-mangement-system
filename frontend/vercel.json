{
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://order-mangement-system.vercel.app/api/:path*"
    }
  ],
  "builds": [
    { "src": "package.json", "use": "@vercel/static-build", "config": { "distDir": "dist" } }
  ]
}